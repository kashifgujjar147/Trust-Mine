import {Navigate,Outlet} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Protected(){const {user,loading}=useAuth();if(loading)return <div className="center">Loading TRUST MINE…</div>;return user?<Outlet/>:<Navigate to="/login" replace/>}export function AdminOnly(){const {user,loading}=useAuth();if(loading)return <div className="center">Loading…</div>;return user?.role==='ADMIN'?<Outlet/>:<Navigate to="/dashboard" replace/>}
