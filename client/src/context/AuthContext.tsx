import {createContext,useContext,useEffect,useState,ReactNode} from 'react';import {authService} from '../services';import {clearToken} from '../services/api';import {User} from '../types';
interface Ctx{user:User|null;loading:boolean;login:(email:string,password:string)=>Promise<void>;register:(d:Record<string,string>)=>Promise<void>;logout:()=>void;}
const C=createContext<Ctx|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const [user,setUser]=useState<User|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem('tm_token')){setLoading(false);return}authService.me().then(setUser).catch(()=>{clearToken();setUser(null)}).finally(()=>setLoading(false))},[]);const login=async(email:string,password:string)=>{const r=await authService.login(email,password);setUser(r.user)};const register=async(d:Record<string,string>)=>{const r=await authService.register(d);setUser(r.user)};const logout=()=>{authService.logout();setUser(null)};return <C.Provider value={{user,loading,login,register,logout}}>{children}</C.Provider>}
export const useAuth=()=>{const c=useContext(C);if(!c)throw Error('AuthProvider missing');return c};
