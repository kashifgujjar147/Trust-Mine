import {FormEvent,ReactNode,useState} from 'react';import {useNavigate,Link,useSearchParams} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {Button,Card,Field} from '../components/ui';import {ShieldCheck} from 'lucide-react';
function AuthBox({title,subtitle,children}:{title:string;subtitle:string;children:ReactNode}){return <div className="auth-page"><Card className="auth-card"><div className="brand big"><span>TM</span><div><b>TRUST MINE</b><small>Mine Today • Earn Tomorrow</small></div></div><div className="auth-trust"><ShieldCheck size={15}/> Secure account workspace</div><h1>{title}</h1><p>{subtitle}</p>{children}</Card></div>}
export function Login(){const [email,setEmail]=useState('admin@trustmine.demo'),[password,setPassword]=useState('Admin@12345'),[error,setError]=useState(''),[loading,setLoading]=useState(false);const {login}=useAuth();const nav=useNavigate();const submit=async(e:FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await login(email,password);nav('/dashboard')}catch(x:any){setError(x?.message||x?.response?.data?.message||'Login failed')}finally{setLoading(false)}};return <AuthBox title="Welcome back" subtitle="Sign in to your TRUST MINE workspace"><form onSubmit={submit} className="form-grid"><Field label="Email"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></Field><Field label="Password"><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></Field>{error&&<p className="error">{error}</p>}<Button type="submit" loading={loading}>Sign in</Button></form><div className="auth-row"><Link to="/forgot-password">Forgot password?</Link><span>Demo: admin@trustmine.demo / Admin@12345</span></div><p className="auth-link">No account? <Link to="/register">Create one</Link></p></AuthBox>}
export function Register(){const [form,setForm]=useState({fullName:'',username:'',email:'',phone:'',password:'',referralCode:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);const {register}=useAuth();const nav=useNavigate();const update=(k:string,v:string)=>setForm(f=>({...f,[k]:v}));const submit=async(e:FormEvent)=>{e.preventDefault();if(form.password.length<8){setError('Password must be at least 8 characters.');return}setLoading(true);try{await register(form);nav('/dashboard')}catch(x:any){setError(x?.message||'Registration failed')}finally{setLoading(false)}};return <AuthBox title="Create account" subtitle="Create a demo-ready account. Backend validation will remain authoritative."><form onSubmit={submit} className="form-grid two"><Field label="Full name"><input value={form.fullName} onChange={e=>update('fullName',e.target.value)} required/></Field><Field label="Username"><input value={form.username} onChange={e=>update('username',e.target.value)} required/></Field><Field label="Email"><input type="email" value={form.email} onChange={e=>update('email',e.target.value)} required/></Field><Field label="Phone"><input value={form.phone} onChange={e=>update('phone',e.target.value)}/></Field><Field label="Password"><input type="password" value={form.password} onChange={e=>update('password',e.target.value)} required minLength={8}/></Field><Field label="Referral code"><input value={form.referralCode} onChange={e=>update('referralCode',e.target.value)} placeholder="Optional"/></Field>{error&&<p className="error full">{error}</p>}<div className="full"><Button type="submit" loading={loading}>Create account</Button></div></form><p className="auth-link">Already registered? <Link to="/login">Sign in</Link></p></AuthBox>}
export function ForgotPassword(){return <AuthBox title="Reset access" subtitle="Password reset delivery is not configured in this build."><div className="notice">No reset email provider is connected, so this action is intentionally unavailable rather than pretending to send a reset message.</div><Button disabled>Reset request unavailable</Button><p className="auth-link"><Link to="/login">Back to sign in</Link></p></AuthBox>}
export function ResetPassword(){const [q]=useSearchParams();return <AuthBox title="Choose a new password" subtitle={`Reset token: ${q.get('token')?'provided':'awaiting token'}`}><div className="notice">Password reset is unavailable until a server-side reset-token and email provider is configured.</div><Button disabled>Update password unavailable</Button><p className="auth-link"><Link to="/login">Back to sign in</Link></p></AuthBox>}
