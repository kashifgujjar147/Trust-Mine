import 'dotenv/config';import {z} from 'zod';export const env=z.object({NODE_ENV:z.string().default('development'),PORT:z.coerce.number().default(5000),MONGODB_URI:z.string().default('mongodb://127.0.0.1:27017/trust-mine'),JWT_SECRET:z.string().min(16).default('dev-only-change-me-1234567890'),CLIENT_URL:z.string().default('http://localhost:5173'),PAYMENT_MODE:z.string().default('manual'),MONGODB_TRANSACTIONS_REQUIRED:z.coerce.boolean().default(false)}).superRefine((v,ctx)=>{if(v.NODE_ENV==='production'&&v.JWT_SECRET==='dev-only-change-me-1234567890')ctx.addIssue({code:z.ZodIssueCode.custom,path:['JWT_SECRET'],message:'JWT_SECRET must be changed in production'});}).parse(process.env);
