import {Router,RequestHandler} from 'express';
import {register,login,me,updateMe} from '../controllers/auth.js';import * as c from '../controllers/core.js';import {auth,admin} from '../middleware/auth.js';import {validate} from '../middleware/validate.js';import {depositSchema,withdrawalSchema,supportSchema} from '../validators/index.js';import {PackagePurchase} from '../models/index.js';
export const router=Router();
const wrap=(handler:RequestHandler):RequestHandler=>(req,res,next)=>Promise.resolve(handler(req,res,next)).catch(next);
const route={get:(path:string,...handlers:RequestHandler[])=>router.get(path,...handlers.map(wrap)),post:(path:string,...handlers:RequestHandler[])=>router.post(path,...handlers.map(wrap)),patch:(path:string,...handlers:RequestHandler[])=>router.patch(path,...handlers.map(wrap)),delete:(path:string,...handlers:RequestHandler[])=>router.delete(path,...handlers.map(wrap))};
route.get('/health',(_r,s)=>s.json({ok:true,service:'trust-mine-api'}));
route.post('/auth/register',register);route.post('/auth/login',login);route.get('/auth/me',auth,me);route.patch('/users/me',auth,updateMe);
route.get('/packages',auth,c.packages);route.get('/packages/:id',auth,c.packageById);route.get('/payment-methods',auth,c.methods);route.get('/users/dashboard',auth,c.dashboard);route.get('/settings',auth,c.settings);
route.post('/deposits',auth,validate(depositSchema),c.createDeposit);route.get('/deposits',auth,c.deposits);route.get('/deposits/:id',auth,c.depositDetails);
route.post('/withdrawals',auth,validate(withdrawalSchema),c.createWithdrawal);route.get('/withdrawals',auth,c.withdrawals);route.get('/withdrawals/:id',auth,c.withdrawalDetails);
route.get('/transactions',auth,c.transactions);route.get('/team',auth,c.team);route.get('/rewards',auth,c.rewards);route.post('/rewards/:id/claim',auth,c.claimRewardRoute);route.get('/promo',auth,c.promos);route.post('/promo/validate',auth,c.promoValidate);route.post('/promo/apply',auth,c.promoApply);route.get('/notifications',auth,c.notifications);route.patch('/notifications/:id/read',auth,c.markNotification);route.get('/support',auth,c.tickets);route.post('/support',auth,validate(supportSchema),c.createTicket);route.get('/package-purchases',auth,async(req:any,res:any)=>res.json({purchases:await PackagePurchase.find({userId:req.user.id}).populate('packageId').sort({createdAt:-1})}));
route.get('/admin/dashboard',auth,admin,c.adminDashboard);route.get('/admin/users',auth,admin,c.adminUsers);route.get('/admin/packages',auth,admin,c.adminPackages);route.post('/admin/packages',auth,admin,c.createPackage);route.patch('/admin/packages/:id',auth,admin,c.updatePackage);route.delete('/admin/packages/:id',auth,admin,c.archivePackage);route.get('/admin/deposits',auth,admin,c.adminDeposits);route.post('/admin/deposits/:id/verify',auth,admin,c.verifyDeposit);route.post('/admin/deposits/:id/reject',auth,admin,c.adminDepositReject);route.get('/admin/withdrawals',auth,admin,c.adminWithdrawals);route.post('/admin/withdrawals/:id/approve',auth,admin,c.adminWithdrawalApprove);route.post('/admin/withdrawals/:id/reject',auth,admin,c.adminWithdrawalReject);route.post('/admin/withdrawals/:id/processing',auth,admin,c.adminWithdrawalProcessing);route.post('/admin/withdrawals/:id/complete',auth,admin,c.adminWithdrawalComplete);
route.get('/admin/transactions',auth,admin,c.adminTransactions);route.get('/admin/payment-methods',auth,admin,c.adminPaymentMethods);route.post('/admin/payment-methods',auth,admin,c.crudPayment);route.patch('/admin/payment-methods/:id',auth,admin,c.crudPayment);route.get('/admin/promo',auth,admin,c.adminPromos);route.post('/admin/promo',auth,admin,c.crudPromo);route.patch('/admin/promo/:id',auth,admin,c.crudPromo);route.get('/admin/rewards',auth,admin,c.adminRewards);route.post('/admin/rewards',auth,admin,c.crudReward);route.patch('/admin/rewards/:id',auth,admin,c.crudReward);route.get('/admin/settings',auth,admin,c.adminSettings);route.patch('/admin/settings',auth,admin,c.updateSettings);route.get('/admin/audit-logs',auth,admin,c.adminAuditLogs);route.get('/admin/notifications',auth,admin,c.adminNotifications);route.get('/admin/support',auth,admin,c.adminTickets);
